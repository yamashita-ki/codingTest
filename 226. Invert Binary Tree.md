## step1 とりあえず解く
- 計算量
  - 時間
  - 空間
- 何も思い浮かばなかった><
- どうやったらDFSできるんだろう,,,で終わってた

```java

```

## step2 他の人の回答を見る
- 計算量
  - 時間:O(N)
  - 空間:O(N)
- 再起的にNodeを見ていくシンプルな回答
- これでよかったのか確かにと納得
- Homebrewの作者がGoogleの面接で解けなかった有名問題らしい

```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */

class Solution {
    public TreeNode invertTree(TreeNode root) {
        if (root == null) {
            return null;
        }
        TreeNode tmpNode = root.right;
        root.right = root.left;
        root.left = tmpNode;
        invertTree(root.left);
        invertTree(root.right);
        return root;
    }
}

```


## step3 3回とく
- step2で解いた